The current [PDF](/home/luke/STA/sta_notes.pdf) has **67 pages**. Its source is [sta_notes.tex](/home/luke/STA/sta_notes.tex). This review includes the full-document audit and refinement of September 15, 2026.

The current convention is `Z'=T^H Z T`, with `R=exp(-i theta u.sigma/2)` and `L=exp(-theta u.sigma/2)`. Rotations use `R^H Z R`; boosts use `L^H Z L`. The rotation gives components in axes rotated by `+theta`, while the active amplitude `psi'=R psi` rotates the state by `+theta`. Boost component results retain the same passive velocity `+beta u`.

The quantum explanation now comes directly from `sc(psi^H Z psi)`: applying `psi'=T psi` inserts `T^H Z T` in the original pairing. Normalized expectations include the transformed norm when `T` is not unitary. The auxiliary polarization paravector `psi psi^H/rho` and its density-matrix development have been removed from the main text, appendix, and equation reference. Polarization and probabilities follow directly from the same pairing; the arbitrary-axis component expansion proves that the local pure-state polarization has unit length.

All dependent formulas use this convention. In particular, the derivative is `partial'=T^-1 partial (T^-1)^H`, the field law is `F'=T^H F (T^H)^-1`, and successive frame factors compose as `T T'`. The right frame generator is `Omega=T^-1(dT/ds)`, with the corresponding conjugations in transport and a plus commutator in curvature. Both interacting Dirac equations transform covariantly with entries `Psi1'=T^-1 Psi1` and `Psi2'=T^H Psi2`. The opposite boost factors agree with [Tong's QFT notes](https://davidtong.org/pdfs/teaching/quantum-field-theory/qft.pdf), printed p. 86, Eq. (4.31).

Current validation: **160 exact boost/covariance checks** and **28 exact curvature checks** pass. An independent direct-paravector check tested **256 arbitrary-axis amplitudes**, including polarization, projector probabilities, active rotations, and normalized general transforms, with maximum numerical discrepancy below `9e-15`. The source has **225 numbered equations**, all referenced, **48 canonically tagged summary formulas**, **273 unique labels**, and **10 bibliography entries** in first-use order. All **116 contents destinations** match the printed pages. The 67-page PDF builds without warnings or overfull/underfull boxes. The changed transformation, geometry, quantum, physics, appendix, and summary pages were visually checked; the compact title and two-page contents remain intact. The eight component underbraces are retained. `tmp.txt` is unchanged.

**Earlier review record.** The following records the preceding passes. Their intermediate transformation conventions and verification counts are superseded by the current audit above.

The document retains the agreed algebra, spacetime, electromagnetism, and original quantum development. `tmp.txt` is unchanged. The quantum discussion covers KG and Dirac with and without potentials, Fourier square roots, characteristic roots, solution reconstruction, gauge and rest phases, energy recursions, Schrödinger and Pauli limits, the original free fourth-power correction, and the necessary spin-half and probability definitions. Previously removed worked examples, higher-spin developments, magnetic duality, constant-field motion, and speculation remain absent.

The order separates algebraic definitions and identities, transformation rules, differential fields, spacetime, electromagnetism, relativistic waves, low-energy limits, spin and probability, and physical Lorentz transformations. Generalized coordinates precede varying frames and the local curved-spacetime foundation at the end of spacetime. Pauli-matrix representation remains the sole appendix; the unnumbered equation reference follows the bibliography.

The preceding refinement clarified these retained definitions and derivations:

- Complex vector dot and cross products are bilinear. Eigenvalues are defined intrinsically through the paravector determinant, and spectral series have explicit convergence conditions.
- Unit, unitary, determinant-one, norm-one, and self-inverse conditions remain distinct. `U` denotes real proper velocity: its inverse is its sigma conjugate, while its Hermitian conjugate is itself. General transformations continue to use `T`.
- The Lorentz sign conventions are consistent across coordinates, derivatives, fields, and force. Local-frame components of physical acceleration are distinguished from derivatives that also differentiate the changing frame.
- Quantum amplitudes are defined before their densities. An arbitrary-axis pair supplies two complex components, and its closure under paravector left multiplication is explicit. The pure-state determinant property is derived before use in the polarization probabilities. No fixed right-projector definition or preferred physical axis is introduced.
- The discrete Fourier expansion is scoped to spatially periodic free KG solutions. Its cyclic wavenumber convention retains all factors of `2pi`. The two initial-data combinations explicitly determine the positive and negative frequency branches.
- Characteristic roots now precede Dirac reconstruction. A scalar KG seed gives a restricted family; the two-component seed gives the complete massive reconstruction. For a general potential, the interacting second-order seed equation still has to be solved.
- Dirac evolution conserves the spatially integrated norm, subject to the stated boundary conditions. Its density is nonnegative, not pointwise conserved. The expectation pairing explicitly permits the complex operators needed in the uncertainty derivation.
- The rest-phase calculation and exact recursions remain intact. The controlled large/small-amplitude reduction gives Pauli; the scalar reduction gives Schrödinger at the same leading order. The next free correction retains its stated approximation conditions.

The numbering pass removed **97 unused equation numbers**, without removing their formulas. Comparing the mathematical source before and after that pass, ignoring numbering commands and whitespace, gives an exact match. Including the curvature development, there are now **224 numbered equations**, all cross-referenced, and **48 equation-summary formulas**, each tagged with a link to its canonical numbered result. The audit finds **272 unique labels**, no missing targets, and no duplicate labels. All **10 bibliography entries** are cited in first-use order; the IEEE-style citations have verified source locators.

The source remains ASCII. Main-text matrix notation is restricted to the paravector block map `W` and two-entry columns; `mat()` appears only in the matrix appendix. Notation checks find no forbidden approximation signs, tensor-product notation, or symmetry-group notation. The appendix verifies the same algebra, arbitrary-axis components, probabilities, characteristic determinant, Dirac equations, standard Dirac basis, low-energy envelopes, and original quaternion correspondence.

Verification completed:

- The preceding full run of `verify_sta.py` passed **184 exact symbolic checks**. It also checks some historical supplemental identities that are no longer in the manuscript.
- Independent targeted reviews pass **16 classical symbolic checks**, **40 arbitrary-axis quantum numerical checks**, and symbolic reconstruction of the KG initial data.
- The final LaTeX build has no warnings, unresolved references or citations, or overfull/underfull boxes. `git diff --check` passes.
- Every page of the preceding 60-page PDF was visually inspected. A repeated inline interacting Dirac equation was replaced with its existing reference to avoid an awkward wrap; the changed page was rebuilt and inspected again. No clipping, equation collisions, or isolated headings remain in the reviewed pages.

The front matter retains the compact title/author/abstract on a dedicated, unnumbered page. The complete contents occupies the next two pages, numbered i--ii; the main text starts at page 1. The title page is unchanged by the boost and curvature revisions. All **116 contents destinations** match the final printed page numbers.

The boost revision compares the promising alternatives explicitly:

| Construction | Result and choice |
| --- | --- |
| Positive half-rapidity exponential with inverse factors | Correct; the earlier PDF used this convention. |
| Negative half-rapidity exponential with direct factors | Adopted: `L=exp(-theta u.sigma/2)`, `X'=L X L^H`, with `L^H=L`. It gives the standard passive frame velocity `+beta u`. |
| Full-rapidity exponential with a gamma prefactor | Correct as `L^2=gamma(1-beta u.sigma)`, but its congruence requires square roots. Using it on both sides doubles rapidity. |
| Similarity for spacetime | Preserves determinant but fixes the scalar part and generally destroys reality. It cannot produce the stated spacetime boost. |
| Positive-norm normalization or nonzero real scalar exponent | Changes the interval scale. A scalar exponent `a` multiplies the transformed determinant by `exp(4a)`, so exact metric preservation requires `a=0`. |

The pure rotation factor is now `R=exp(-i theta u.sigma/2)` so both transformations use `X'=T X T^H`. This is a convention change, with the same right-handed rotation and passive boost components as before. Spin amplitudes consequently use `psi'=R psi`. Proper velocity remains `U=gamma(1+v.sigma)`; for the comoving boost it is `U=L^-2`, giving `L U L^H=1`. The Cayley relation, constant-acceleration tangent, varying-frame derivatives, quaternion correspondence, and equation reference were updated consistently.

The development now proves the scalar-exponent restriction, the distinction between determinant and positive norm, the associated real metric pairing, and boost/rotation factorization. It derives the half-angle gamma factor, longitudinal exponential scaling, full interval and light-cone invariance, coordinate and world-line transformations, time dilation, simultaneous length measurements at arbitrary orientation, and energy--momentum transformations. The field similarity `F'=T F T^-1` follows from the transformed potential and coordinate chain rule. Its electric and magnetic components preserve `E^2-B^2` and `E.B`; their positive norm is the observer-dependent energy density. The stress map and real Lorentz force transform consistently. A new subsection in the existing matrix appendix verifies the arbitrary-axis boost and rotation matrices.

The new [verify_boosts.py](/home/luke/STA/verify_boosts.py) passes **129 exact checks**, including symbolic arbitrary-axis identities, exact composite frames, potential-to-field and Maxwell covariance at transformed coordinates, and first and second varying-frame derivatives. Separate reviews passed **360 algebraic numerical checks** and **3,200 physical numerical assertions** across arbitrary axes. Primary-source locators were rechecked against Tong's electromagnetic and relativity notes and Baylis--Sobczyk. The original text's spacetime similarity was not a valid coordinate boost; the corrected earlier PDF's inverse congruence was valid and is now expressed in the simpler direct convention.

The boost-revision validation found no missing or duplicate references, unused equation numbers, bibliography-order errors, LaTeX warnings, or overfull/underfull boxes. All changed or repaginated pages were visually inspected; unchanged opening algebra pages were checked against the preceding PDF. The final five pages affected by convention cleanup or wording were rendered and inspected again. That revision produced **62 pages**.

The curved-spacetime review confirms that the original generalized-coordinate and varying-frame equations are algebraically correct. Their limitation is physical scope. If the interval is obtained from the differential of a single inertial-position function with nonsingular Jacobian, it is flat Minkowski spacetime expressed in different coordinates. If a local algebra basis is obtained by similarity, its generator products remain unchanged; its possible complex coefficients do not establish a curved real spacetime metric. Real spacetime components require the separately defined congruence action.

There is a necessary distinction between one curve and a whole coordinate domain. Along one curve, any smooth Lorentz transport can be written as `Omega = (dT/ds) T^-1`, including transport in curved spacetime. This follows by solving `dT/ds = Omega T`. But if one smooth endpoint function `T(X)` supplies every directional connection, `Omega(A) = (partial_A T) T^-1`, then commuting coordinate derivatives makes

\[
\partial_A\Omega(B)-\partial_B\Omega(A)-[\Omega(A),\Omega(B)]=0.
\]

That is zero curvature, not a derivation of gravity. Curved transport depends on the path. A varying boost, varying rotation, nonzero coordinate acceleration, or varying metric coefficient alone is not a curvature test.

New Section 6.7, printed pages 28--31 in the current PDF, supplies the missing local construction without tensor notation. Its coordinate directions are constant real paravectors. The real-linear, invertible frame map `H(X,A)` sends coordinate increments to local physical increments, defining `ds^2 = det(H(dX))`. It is a map, not a single multiplying paravector, and need not preserve the coordinate determinant. A general local metric of signature `(+,-,-,-)` can be represented this way. The existing determinant-one transformations still act on the resulting local frame and preserve its physical interval.

The complex pure-vector map `Omega(X,A)` defines comparison between neighboring coordinates. Subtracting its congruence action from the ordinary directional derivative gives the covariant derivative. Its pure-vector restriction guarantees metric compatibility and removes an otherwise invisible imaginary scalar freedom. The explicitly defined torsion-free condition uniquely determines this connection from `H`; the manuscript states its concrete solution as 24 real linear equations for 24 real coefficients. This is the usual metric-compatible, torsion-free geometry, rather than an unconstrained connection with additional torsion.

The derivative commutator gives the curvature directly in the existing algebra. The manuscript derives its transformation under a local `T(X)`, showing that a change of frame cannot remove nonzero curvature. This curvature formula assumes constant coordinate directions; variable directions would require additional terms. The congruence derivative is also distinguished from the commutator derivative appropriate to similarity-transforming fields, so an ordinary product rule cannot be carried across the two representations without checking it.

Free fall follows by varying the proper-time action with fixed endpoints. The real local tangent is `Z = H(dX/ds)`, with determinant one and positive scalar part selecting the future direction. It is generally different from the coordinate derivative `dX/ds`; the existing flat-spacetime symbol `U` has not been silently redefined. The variational calculation gives the standard equation that the local tangent is parallel transported along its path. Proper time is chosen on the reference curve after taking the variation.

The worked example uses an arbitrary constant real unit direction `u`, `z = u.r`, and a smooth positive real function `f(z)`. For `ds^2 = f(z)^2 dt^2 - dr^2`, the compatible connection is proportional to `f'`, while curvature is proportional to `f''`. Therefore `f = 1 + a z` has accelerated coordinates but zero curvature; `f'' != 0` has nonzero curvature. Both the scalar and vector free-fall equations are derived and agree with direct variation of this metric. This is a geometry example, not a claim to solve Einstein's equation for an unspecified matter source.

The answer to whether this supports GR is therefore **yes, as a local geometric foundation**. It now provides intervals, transport, curvature, and geodesics. It does not yet supply Einstein dynamics. The next development would define curvature contractions and the matter action, obtain the field equation from the gravitational action, and verify a standard solution and its Newtonian limit. None of those field equations follows from determinant preservation alone. The main text explicitly preserves the flat inertial-coordinate scope of the following electromagnetic and quantum equations.

The physical comparison uses [Tong's General Relativity notes](https://www.davidtong.org/pdfs/teaching/general-relativity/gr.pdf), pp. 108--110, 123--127, and 132--136, and [Francis and Kosowsky](https://arxiv.org/pdf/gr-qc/0311007), Sec. II. The latter explicitly develops standard GR through algebraic frames and connections. The manuscript defines its own signs and paravector actions rather than copying notation from either source.

The new [verify_curvature.py](/home/luke/STA/verify_curvature.py) passed **25 exact symbolic checks** using an independent Pauli-matrix representation internally. They cover the original first and second frame derivatives, a noncommuting pure-frame example, independent curved connections, metric compatibility, local-frame covariance, the arbitrary-direction example, free-fall components, and determinant conservation. A separate mathematical review verified the variational sign and the existence and uniqueness of the compatible connection. The final **66-page** PDF builds without warnings or overfull/underfull boxes. The title and contents and every changed or repaginated page from physical page 29 onward were visually inspected; references and all contents destinations were audited again.

The subsequent notation refinement retains the common Hermitian congruence explicitly in both rotation and boost laws: `Z'=R Z R^H` and `Z'=L Z L^H`. The properties `R^H=R^-1` and `L^H=L` are recalled beside their derivations. The same notation now appears in coordinate increments, world-line integrals, momenta, force and stress transformations, the matrix appendix, and the equation reference. Inverse coordinate transformations and the derivative follow the corresponding general `T` laws with their conjugations in the correct order. Electromagnetic field similarity remains `F'=L F L^-1`. This changes presentation only; the exponents, physical signs, component results, and equation numbering are unchanged. The rebuilt PDF remains 66 pages, with clean LaTeX and reference audits.

The final full-document pass reviewed algebra, classical physics and curved geometry, and quantum mechanics independently, then checked the appendix and equation reference. It preserves the retained definitions, derivations, and results. The existing order keeps Dirac immediately before its energy recursion and low-energy limits. The following refinements close remaining gaps without introducing new notation:

- The unit definition now explicitly connects invertibility to a nonzero determinant. A redundant projector factor is removed, and the Cayley and logarithm descriptions identify precisely which scalar or vector term is meant.
- The velocity-turning commutator sits beside the acceleration product. The field classification is explicitly pointwise, with the two subluminal boost velocities that eliminate the magnetic or electric field when the stated invariants allow it.
- The field action recalls its conserved current; the energy-balance prose identifies the signed power delivered to matter. The KG potential subsection recalls its real potential paravector and gauge scalar.
- Spin coefficient probabilities explicitly require the measurement axis to match the chosen basis axis. The polarization determinant argument no longer silently reuses that axis, and the uncertainty derivation states its normalization and expectation notation.
- The Tong bibliography links use the current author-hosted PDFs. The Poynting result has an exact page and equation locator.

All **338 symbolic checks** passed again: 184 in the existing algebra/quantum verification, 129 in boost verification, and 25 in curvature verification. The final source has **224 numbered equations**, all referenced, **48 summary formulas** linked to their canonical equations, **272 unique labels**, and **10 bibliography entries** in citation order. No missing references, duplicate labels, LaTeX warnings, or overfull/underfull boxes remain.

Every page of the rebuilt PDF was visually inspected. This found and corrected two layout defects: the quantum TOC block split across pages, and two adjacent spacetime displays ran together. The five pages affected by the final fixes were rendered and checked again. All **116 contents destinations** match the printed pages. The compact title and abstract are unchanged, and the contents still occupies exactly two pages. The final PDF has **67 pages**; `tmp.txt` is unchanged.

The equation-introduction pass checked all **434 display groups**. Missing lead-ins were restored in the acceleration and mechanical-work derivations, unit-definition bullets, and equation reference. Existing introductions now govern consecutive equations with commas between them and a final full stop; trailing qualifications were moved before the relevant displays. Comparing every display against the preceding source confirms that all mathematics and labels are unchanged apart from punctuation and whitespace. All **22 changed PDF pages** were visually inspected. The document remains **67 pages**, with a clean build and reference audit.

These checks support the retained derivations and presentation; they do not assert an independent formal proof of every physical statement.
